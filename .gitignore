# --- Python ---
__pycache__/
*.pyc
.pytest_cache/
build/
dist/
*.egg-info/
.venv/

# --- data: raw corpora are downloadable, tokenized files are regenerated by `python3 data.py <name>` ---
data/**/train.bin
data/**/val.bin
data/**/meta.json
data/text8/text8
data/text8/text8.zip
# keep data/tinyshakespeare/input.txt (1 MB): the PRD's dataset, so a fresh clone runs the control immediately

# --- results: entire directory (checkpoints, logs, metrics, stdout captures); evals.md keeps the summary ---
results/

# --- secrets and local state ---
# per-worker credentials (auth.py); never commit
workers.json
*.tok
nohup.out
.DS_Store
.env

# --- scratch ---
/tmp/

# --- figures: regenerated by `python3 make_figures.py` / `python3 plots.py` ---
figures/

# --- personal / working documentation (not part of the published project) ---
decentralized-training-mvp-prd.md
WRITEUP.md
qna.md
roadblocks.md
evals.md
scripts/make_report.py


# --- interview study tool (generated artifact, personal) ---
code_explorer.html
scripts/build_explorer.py
slides.pptx
scripts/build_slides.py
